class State {
	constructor() {
		this.area = new Area()
	}

	init() {
		this.area.add(new Level())
	}

	destroy() {
		this.area.destroy()
	}

	update(dt) {
		this.area.update(dt)
	}

	render() {
		this.area.render()
	}
}