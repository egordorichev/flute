class Component {
	constructor(name) {
		if (name !is String) {
			Fiber.abort("Expected component name")
		}

		this.id = name
	}

	init() {

	}

	destroy() {
		
	}

	update(dt) {

	}

	handleEvent(e) {
		
	}

	sendEvent(e) {
		this.entity.handleEvent(e)
	}
}